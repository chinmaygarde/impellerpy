# CI Rules Auditor Memory

- [impellerpy build and CI conventions](project_build_ci_conventions.md) — build system is justfile over CMake; CI inlines uv commands directly rather than calling just; cibuildwheel handles all wheel packaging
